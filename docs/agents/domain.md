# Domain docs

Use a single-context layout: root `CONTEXT.md` for domain terms and root `docs/adr/` for architecture decisions. Read relevant files before changing the corresponding area. If a file does not exist yet, continue; the domain-modeling skill creates it when needed.

Use terms from `CONTEXT.md` in issues, proposals, and code. Surface any conflict with an existing ADR explicitly.
